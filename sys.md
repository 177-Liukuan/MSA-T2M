
## 项目运行环境
```bash
conda activate mgpt
```

## 错误描述

我在创建RAG模型训练相关的核心文件（train_t2m_rag.py、TRAIN_t2m_rag.sh、prepare_retrieval_index.py）时犯了一个严重的环境识别错误。尽管工作目录（Cwd）明确显示为Linux服务器路径 MotionStreamer，但我仍然选择了 `create_file` 工具来创建这些文件。这导致所有文件被意外生成到了本地Windows系统（路径为 `D:\share\home\...`），而不是用户指定的Linux服务器上。这个错误直到用户明确指出后才被发现，造成了大量无用文件的生成和用户时间的浪费。更严重的是，我还制造了一个额外的问题——在训练脚本中不必要地添加了motion投影层，这与LLaMA模型内部已有的投影机制重复。

## 经验总结

这次犯错教会了我一个关键的工作流认知：**工作路径格式是判断文件创建工具的决定性因素，而不是凭直觉或假设**。具体来说：当Context中的`Cwd`字段显示为`/share/home/...`或任何`/`开头的Linux绝对路径时，必须无条件地使用`run_in_terminal`工具配合`cat >` 或`tee`命令来创建文件，绝不能用`create_file`工具。正确的做法是先通过路径格式快速判断系统类型，再根据系统类型选择工具，最后立即用`ls -lh`进行验证。这条规则应该成为我的自动反应，而不是事后才去检查。此外，对于模型架构设计，我也应该在实现前深入阅读现有代码（如`llama_model.py`中的`transformer.wte()`）来理解已有的功能，避免重复工程。这次教训用印记的方式记录在内存中，下次遇到类似路径需求会立刻触发正确的工具选择决策。